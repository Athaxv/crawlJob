{
  "name": "crawljob",
  "version": "1.0.0",
  "description": "Local Playwright automation for one-click job applications on Wellfound and Instahyre.",
  "type": "module",
  "bin": {
    "crawlJob": "./bin/crawlJob.mjs"
  },
  "scripts": {
    "crawlJob": "bun run build && node dist/cli.js",
    "start:wellfound": "bun run build && node dist/cli.js run --site wellfound",
    "start:instahyre": "bun run build && node dist/cli.js run --site instahyre",
    "build": "tsc",
    "start:node": "node dist/cli.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "ora": "^9.4.1",
    "picocolors": "^1.1.1",
    "playwright": "^1.62.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.9.3"
  }
}
